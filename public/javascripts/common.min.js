!(function (a) {
  a('body').removeClass('no-javascript').addClass('used-javascript'),
    a('.supplier-tabs li a').click(function (b) {
      b.preventDefault(),
        a('.supplier-tabs li').removeClass('active'),
        a(this).parent().addClass('active'),
        a(this).tab('show');
    });
  var b = new a.DEVFUNC.accordion();
  b.handleAccordion(),
    a.DEVFUNC.spMenu({
      menuBtn: [{ oBtn: '#btn-nav-sp a', target: '#sub-menu-sp' }],
      closeBtn: '.close_btn',
      addClass: 'spmenu_open',
    }),
    (a.HANDLEBREAKPOINT = function (b) {
      a.DEVFUNC.elemMove(a.GSET.MOVE_ELEM, b),
        a('.partner-wrap .partner-slides').length && partner_slides.resize(b);
    }),
    a.DEVFUNC.MATCHMEDIA();
  var c = !1;
  a(window).resize(function () {
    c !== !1 && clearTimeout(c), (c = setTimeout(function () {}, 0));
  });
})(jQuery);
