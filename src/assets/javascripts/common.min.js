!(function (a) {
  function b() {
    a('.milestones-diagram .diagram-col .diagram-item').each(function (b, c) {
      var d = a(this).find('.diagram-unit').offset(),
        e = a(this).find('.diagram-unit').attr('data-type');
      console.log(d),
        a(this)
          .find('.diagram-unit')
          .hover(
            function () {
              a('.diagram-popup').fadeIn(350),
                a('.diagram-popup p').text(e),
                a('.diagram-popup').css({
                  left: d.left - 133,
                  top: d.top + 88,
                });
            },
            function () {
              a('.diagram-popup').hide();
            },
          );
    });
  }
  a('body').removeClass('no-javascript').addClass('used-javascript'),
    a('.supplier-tabs li a').click(function (b) {
      b.preventDefault(),
        a('.supplier-tabs li').removeClass('active'),
        a(this).parent().addClass('active'),
        a(this).tab('show');
    }),
    b();
  var c = new a.DEVFUNC.accordion();
  c.handleAccordion(),
    a.DEVFUNC.spMenu({
      menuBtn: [{ oBtn: '#btn-nav-sp a', target: '#sub-menu-sp' }],
      closeBtn: '.close_btn',
      addClass: 'spmenu_open',
    }),
    (a.HANDLEBREAKPOINT = function (b) {
      a.DEVFUNC.elemMove(a.GSET.MOVE_ELEM, b),
        a('.partner-wrap .partner-slides').length && partner_slides.resize(b);
    }),
    a.DEVFUNC.MATCHMEDIA();
  var d = !1;
  a(window).resize(function () {
    d !== !1 && clearTimeout(d),
      (d = setTimeout(function () {
        b();
      }, 0));
  });
})(jQuery);
